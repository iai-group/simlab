import { Container } from "react-bootstrap";

const Leaderboard = () => {
  return (
    <Container>
      <h3>Leaderboard</h3>
      {/* TODO
        - Add matrix results for user simulators and conversation recommender systems pairs
      */}
    </Container>
  );
};

export default Leaderboard;
