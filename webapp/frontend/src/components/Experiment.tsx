import { Container } from "react-bootstrap";

const Experiment = () => {
  return (
    <Container>
      <h1>Experiment</h1>
      {/* TODO
        - Add a form to create a new experiment
        - Display a list of experiments
          - Link to the experiment detail page
          - Add a button to delete the experiment
      */}
    </Container>
  );
};

export default Experiment;
