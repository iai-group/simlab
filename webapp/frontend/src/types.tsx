export type Task = {
  id: string;
  name: string;
  description: string;
};

export type Metric = {
  id: string;
  name: string;
  description: string;
};

export type Agent = {
  id: string;
  name: string;
  description: string;
};

export type Simulator = {
  id: string;
  name: string;
  description: string;
};
